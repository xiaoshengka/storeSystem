# v0.7.0 MySQL 性能报告

> 本文件只记录实测结果。未执行项填写“未测试”，不得用估算值替代。

## 环境

- 日期与提交：2026-08-22；提交号未记录
- Ubuntu / kernel：VMware Ubuntu；精确版本/kernel 未随结果同步
- CPU / vCPU / 绑核：8 vCPU；未绑核
- 内存：12 GB
- 磁盘、文件系统、虚拟化：VMware；磁盘与文件系统未记录
- GCC、编译参数、allocator、`MYSQL=1` 客户端版本：Makefile 默认 `-O2`、jemalloc；
  精确 GCC/libmysqlclient 版本未随结果同步
- MySQL Server 版本、buffer pool、redo/binlog、数据目录：MySQL 8.0.46；buffer pool
  不在轮次间清空；其余未记录
- 客户端/服务/MySQL 所在主机：同一 Ubuntu VM，TCP loopback
- AOF 路径、fsync 策略：每轮临时空 AOF，`everysec`；计时负载为只读 GET

## 固定 workload

- 32 个持久连接；64B value；90% GET / 10% SET；P16、P64。
- 每种场景 5 轮，报告后四轮或预先约定的 5 轮中位数，并保留每轮原始 CSV。
- 记录 keyspace、预热次数、总请求数、持续时间、命中率和固定到达率设置。

| 场景 | Pipeline | run | QPS | P50 ms | P99 ms | P99.9 ms | max ms | CPU | RSS | 备注 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| MySQL 关闭基线 | 16 | 1–5 | | | | | | | | |
| MySQL 开启、100% 内存命中 | 16 | 1–5 | | | | | | | | |
| MySQL 关闭基线 | 64 | 1–5 | | | | | | | | |
| MySQL 开启、100% 内存命中 | 64 | 1–5 | | | | | | | | |
| 热 key AOF 写回 | 16 | 1–5 | | | | | | | | |
| 热 key AOF 写回 | 64 | 1–5 | | | | | | | | |

## 门禁计算

| 门禁 | 基线中位数 | MySQL 中位数 | 变化 | 目标 | 结论 |
| --- | ---: | ---: | ---: | ---: | --- |
| 100% hit QPS | | | | ≥ -5% | 未测试 |
| 100% hit P99 | | | | ≤ +10% | 未测试 |
| 热写回 QPS | | | | ≥ -10% | 未测试 |
| 热写回 P99 | | | | ≤ +20% | 未测试 |

执行命令：

```bash
KVSTORE_MYSQL_TEST_PASSWORD='kvstore_test_only' \
make mysql-performance-gate
```

脚本会生成 `.summary.csv`、`.gate.json` 与 `.metadata.json`；只有所有分组 CV 和四项
QPS/P99 阈值同时通过，才能把上表“未测试”改为“通过”。

## Miss、合并与故障

| 场景 | 并发/热点度 | 对象规模 | QPS | P50/P99/P99.9 | MySQL queries | 合并率 | 错误 | 备注 |
| --- | --- | --- | ---: | --- | ---: | ---: | ---: | --- |
| String miss smoke | 32 连接、唯一 key、P1 | 10,000 × 64B | 29,024.98 | — / 1.804 / — ms | 10,000 | 0% | 0 | 1 轮，完整校验 PASS |
| String miss 正式 | 32 连接、唯一 key、P1 | 100,000 × 64B | 26,868.13 | — / 1.965 / — ms | 100,000/轮 | 0% | 0 | 5 轮中位数；QPS CV 5.67% |
| Hash 完整加载 | | | | | | | | |
| ZSet 完整加载 | | | | | | | | |
| 同 key 热点合并 | | | | | | | | |
| MySQL 运行期断开 | | | | | | | | |
| 重连与水位追平 | | | | | | | | |

附上测试前后 `INFO CACHE`、`INFO PERSISTENCE`、`INFO MYSQL`，以及 MySQL status、
慢查询、磁盘延迟和服务端日志。结论需区分统计波动、客户端饱和、服务端 CPU 饱和与
MySQL/磁盘瓶颈。

### String 全量冷 miss 逐轮结果

| run | QPS | P99 ms | validation |
| ---: | ---: | ---: | --- |
| 1 | 26,491.81 | 1.965 | PASS |
| 2 | 27,525.39 | 1.850 | PASS |
| 3 | 26,987.51 | 1.973 | PASS |
| 4 | 26,868.13 | 1.901 | PASS |
| 5 | 23,343.86 | 2.179 | PASS |
| **中位数** | **26,868.13** | **1.965** | **PASS** |

结果仅代表 kvstore 内存 Cache 冷、MySQL buffer pool 热的回源场景。当前没有 P50、
P99.9、CPU、RSS、磁盘延迟及完整 metadata 文件的仓库副本；这些字段不能从终端摘要
反推。100% 内存命中、热写回、热点合并和故障性能仍为“未测试”，因此性能门禁尚未
闭环。
